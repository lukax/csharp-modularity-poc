<UserControl x:Class="LOB.UI.Core.View.Controls.Main.ColumnToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Grid.Column="1" MinWidth="70" HorizontalAlignment="Center">

        <controls:Tile FontSize="14" Width="48" Height="48" Margin="0,20,0,0"
                       Background="Transparent" ToolTip="Operações" Command="{Binding OpsCommand}">
            <Image Source="/Resources/Ops.png" />
        </controls:Tile>
        <controls:Tile FontSize="14" Width="48" Height="48" Margin="0,10,0,0"
                       Background="Transparent" ToolTip="Vendas" Command="{Binding ShopCommand}">
            <Image Source="/Resources/Shoppingcart48.png" />
        </controls:Tile>
    </StackPanel>
</UserControl>